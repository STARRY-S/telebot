package utils

const (
	ReplyPermissionDenied = "permission denied"
	ReplyFailed           = "failed"
	ReplyOnlyPrivate      = "private chat only"
	ReplyOnlyGroup        = "group only"
	ReplyOnlyChannel      = "channel only"
)
